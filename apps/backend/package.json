{
  "name": "greatfrontend-backend",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "kill-port": "bun scripts/kill-port.ts",
    "dev": "bun run kill-port -- 3000 && bun --hot src/main.ts",
    "build": "bun build src/main.ts --target bun --outdir dist --external @nestjs/* --external reflect-metadata --external rxjs",
    "start": "bun run kill-port -- 3000 && bun dist/main.js",
    "lint": "bun eslint . --ext .ts",
    "db:generate": "bun drizzle-kit generate",
    "db:push": "bun drizzle-kit push",
    "db:studio": "bun drizzle-kit studio"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.20",
    "@nestjs/core": "^10.4.20",
    "@nestjs/platform-express": "^10.4.20",
    "drizzle-orm": "^0.44.5",
    "postgres": "^3.4.7",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.9.2",
    "@typescript-eslint/eslint-plugin": "^8.41.0",
    "@typescript-eslint/parser": "^8.41.0",
    "eslint": "^9.34.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "prettier": "^3.6.2"
  }
}
